# 通用RAG系统配置 - OpenAI兼容API
# 复制到项目根目录并重命名为 .env.local.universal

# 通用RAG系统配置
UNIVERSAL_ENABLED=true
UNIVERSAL_API_KEY=your_api_key_here
UNIVERSAL_BASE_URL=https://api.openai.com/v1
UNIVERSAL_MODEL=gpt-3.5-turbo
UNIVERSAL_USER_ID=user123

# 使用说明:
# 1. 这个配置适用于任何OpenAI兼容的API
# 2. 可以连接到自部署的RAG服务
# 3. 将your_api_key_here替换为实际API密钥

# 示例用途:
# - 连接到自建的RAG服务
# - 使用vLLM部署的模型
# - 连接到其他兼容OpenAI API的服务
