# 评价器配置 - OpenAI (Chat + Embeddings)
# 复制到项目根目录并重命名为 .env.local.evaluator

# OpenAI配置
OPENROUTER_API_KEY=your_openai_api_key_here
EVALUATOR_BASE_URL=https://api.openai.com/v1
EVALUATOR_MODEL=gpt-3.5-turbo

# Ollama配置 (用于Embeddings，因为OpenAI Embeddings较贵)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=nomic-embed-text:latest

# 使用说明:
# 1. 获取OpenAI API密钥: https://platform.openai.com/api-keys
# 2. 安装并启动Ollama: https://ollama.ai/
# 3. 拉取嵌入模型: ollama pull nomic-embed-text:latest
# 4. 将your_openai_api_key_here替换为实际密钥
# 
# 注意: 如果要使用OpenAI Embeddings，需要修改代码中的Embeddings配置
